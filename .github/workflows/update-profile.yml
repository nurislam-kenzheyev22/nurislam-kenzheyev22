name: Update Profile

on:
  schedule:
    # Обновляем каждый день в 00:00 UTC
    - cron: '0 0 * * *'
  workflow_dispatch: # Позволяет запускать вручную

jobs:
  update-profile:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout repository
      uses: actions/checkout@v3
      
    - name: Update README
      run: |
        # Обновляем дату последнего обновления
        sed -i "s/\*Последнее обновление: .*\*/\*Последнее обновление: $(date '+%B %Y')\*/" README.md
        
    - name: Commit and push changes
      run: |
        git config --local user.email "action@github.com"
        git config --local user.name "GitHub Action"
        git add README.md
        git diff --quiet && git diff --staged --quiet || git commit -m "🔄 Автоматическое обновление профиля"
        git push
